# Xiangdongqu Server Memory

When working in this repository, if the user mentions the Aliyun/Alibaba Cloud server, domain, DNS, HTTPS certificate, systemd service, self-hosted deployment, Workbench deployment, or production server configuration, read this file before acting:

```text
C:\Users\admin\Documents\xiangdongqu\SERVER_DEPLOYMENT_ALIYUN.md
```

The migration-safe Codex memory copy is:

```text
C:\Users\admin\.codex\memories\xiangdongqu-aliyun-server.md
```

If any server, DNS, certificate, deployment command, service file, environment variable name/path, verification result, or production architecture changes, update both copies. Never write real API keys, passwords, tokens, or Git credentials into these memory files.
