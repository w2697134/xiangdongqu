# 智能体会话 ID 记录

用途：记录当前项目相关的长期会话/智能体 ID。后续如果总控会话更换，可以先读取本文件，再尝试恢复或联系对应会话。

## 项目路径

```text
C:/Users/admin/Documents/New project 13
```

## 代码会话

- ID：`019e2aef-d102-73d1-864c-67fa60b23dda`
- 角色：代码实现会话
- 当前职责：实现 Next.js + TypeScript + Tailwind 项目，按阶段完成前台、后台、API、数据库等开发任务。
- 最近任务：阶段 1 验收返工，切换正式生成资产、补待放占位、修移动端标题拥挤、重新运行 lint/build。

## UI / 图片设计会话

- ID：`019e2af5-de8e-7353-906d-7c39a4c61220`
- 角色：UI 视觉与图片生成会话
- 当前职责：规划和生成 Logo、首页主视觉、栏目封面、资料库封面、视频封面等视觉资产。
- 硬性注意：后续只能作为调度会话，必须派子智能体/worker 生成图片、保存到 `design-assets/generated` 和 `public/generated`、更新 `DESIGN_ASSET_MANIFEST.md`，主 UI 会话只回路径和用途，避免把大量图片堆在聊天框。

## 视觉验收与拼装会话

- ID：`019e2b6e-7d90-7ad2-86f5-283a2706533d`
- 角色：视觉细调、资产拼装、拼装后视觉验收会话
- 当前职责：把 UI 会话生成的分层资产放到正确页面位置，检查组合后的首页和关键页面是否真的成立；发现不合格资产或不协调的组合后，反馈给总控，由总控再指挥 UI 会话重做。
- 硬性注意：不能生成任何图片，不能调用图片生成工具。只有 UI / 图片设计会话可以调度生图。视觉验收与拼装会话只能拼装、截图、验收和反馈。
- 硬性注意：不能只验收单张资产，必须验收“资产放进页面后的效果”。任何重做资产重新放入页面后，都必须再次截图验收，不能因为资产重做过就默认合格。
- 边界：该会话负责细调和阶段视觉验收；总控会话负责最终验收和是否进入下一阶段。

## 验收复核子智能体

- ID：`019e2b10-fa38-7c51-8c1d-e3ff5a167270`
- 角色：阶段验收复核，只读检查
- 最近结论：阶段 1 基本通过，但指出正式生成资产未被代码引用，需要返工。

## 使用方式

如果需要继续指挥已有会话，可以尝试：

1. 恢复对应 ID。
2. 发送明确任务。
3. 要求对方读取当前项目文档后再执行。

关键项目文档：

- `CODE_SESSION_BRIEF.md`
- `XIANGDONGQU_PLAN.md`
- `STAGE_1_ACCEPTANCE.md`
- `DESIGN_IMAGE_WORKFLOW.md`
- `IMAGEGEN_EXECUTION_BRIEF.md`
- `DESIGN_ASSET_MANIFEST.md`
- `DESIGN_ROUND_1_REVIEW.md`
- `UI_VISUAL_STANDARD.md`
